this is where we will have all the mobile services
Typscript

## Docker

Placeholder HTTP service on port **3001**. Start with the root compose stack:

```bash
docker compose --env-file .env.docker up mobile
```
